using GeometryTypes: Point #TODO: define this internally

# struct Point{N,T} <: AbstractVector{T}
#     coords::NTuple{N,T}
# end
# Base.size(pt::Point{N,T}) where {N,T} = (N,)
# Base.getindex(pt::Point,i) = getindex(pt.coords,i)

